# Copy to .env and fill in your key (never commit the real .env).
# Get a free key at https://openrouter.ai/keys  (no credit card for free models)
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional overrides (defaults shown):
# OPENROUTER_MODEL=openrouter/free   # auto-routes over available free models
# TOP_K=5
# RATE_LIMIT_PER_MINUTE=15
# RATE_LIMIT_PER_DAY=200
